# cPanel Migration Worker Python dependencies

# Django and database
django>=6.0,<7.0  # Must match kubepanel dashboard version
pymysql>=1.1.0  # MIT licensed MySQL/MariaDB driver (same as kubepanel dashboard)

# Kubernetes client
kubernetes>=28.0.0

# HTTP client for cPanel API
requests>=2.31.0

# Encryption for API token decryption (Apache 2.0 / BSD licensed)
cryptography>=41.0.0

# Legacy crypt module for password hashing (used by kubepanel secrets.py)
legacycrypt>=0.3

# Pin urllib3 to match dashboard (avoids SSL issues with K8s API)
urllib3==2.3.0

# Password hashing
passlib>=1.7.0

# Additional dependencies from kubepanel dashboard
pyotp
qrcode[pil]
geoip2
cloudflare
redis
